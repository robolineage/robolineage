# RoboLineage Frontend

Vue lifecycle console for robot onboarding, task setup, rollout review, health, training, evaluation, and Master Agent summaries.

```bash
npm install
npm run dev
npm run build
```

The frontend expects the FastAPI backend at `http://localhost:8080` and the health service at `http://localhost:8081`.