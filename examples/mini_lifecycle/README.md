# Mini Lifecycle Example

This folder contains a compact artifact trace. It is intentionally small: the
goal is to show the file format and parent-link structure, not to provide a
runtime demo or benchmark.

Read `artifact_trace.example.json` from top to bottom. Each artifact links to
the artifacts it depends on through `parents`.
