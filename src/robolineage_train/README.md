# robolineage_train

Training integration for RoboLineage. The package discovers existing training repositories, prepares dataset adapter plans, launches configured training commands, monitors logs, writes dataset health reports, and binds checkpoints to `policy.meta.json`.