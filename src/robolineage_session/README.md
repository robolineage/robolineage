# robolineage_session

Session API, lifecycle state machine, event log, SSE stream, and rollout archive helpers. Production orchestration is handled by `robolineage_app`; this package provides stable session primitives.