# robolineage_schemas

JSON schemas and validation helpers for lifecycle artifacts. Use this package when reading artifacts from disk, receiving HTTP/SSE payloads, or writing JSON that becomes lifecycle state.

```python
from robolineage_schemas import validate, write_validated_json_atomic
```