# robolineage_data_source Tests

Tests mirror the data-source package. Important coverage includes `rosbag/test_raw_recorder.py`, profile-driven ROS2 configuration, `ros2_profile` adapter behavior, and orchestrator wiring.